// Site configuration settings
const siteConfig = {
    // Layout settings
    layout: {
        // Header spacing (px)
        headerSpacing: 0,
        
        // Content spacing (px)
        contentSpacing: 0,
        
        // Card spacing (px)
        cardSpacing: 8
    },
    
    // Theme settings
    theme: {
        // Default theme (light or dark)
        defaultTheme: 'dark'
    }
};

export default siteConfig;